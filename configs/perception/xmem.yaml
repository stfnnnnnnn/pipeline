# XMem VOS propagation configuration
enabled: true
repo_path: "XMem"
model_path: "models/checkpoints/xmem/XMem-s012.pth"
device: "cuda"

# Inference memory settings
mem_every: 5
deep_update_every: -1
max_mid_term_frames: 10
min_mid_term_frames: 5
max_long_term_elements: 10000
num_prototypes: 128
top_k: 30
disable_long_term: false
